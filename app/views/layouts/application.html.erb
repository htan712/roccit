<!DOCTYPE html>
<html>
<head>
  <title>Roccit</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
   <%= stylesheet_link_tag    "application", media: "all" %>
   <%= javascript_include_tag "application" %>
   <%= csrf_meta_tags %>
</head>
<body>
  <div class="container">
  	<ul class="nav nav-pills">
  		<li id="navi"><%= link_to "Home", root_path %></li>
      <li id="navi"><%= link_to "Topics", topics_path %></li>
  		<li id="navi"><%= link_to "About", about_path %></li>
        <% if current_user %>
           <li id="sign-out"> <%= link_to "Sign Out", session_path(current_user), method: :delete %> </li>
           <li id="sign-out"> <%= current_user.name %> </li>
         <% else %>
           <li id="session"> <%= link_to "Sign Up", new_user_path %> </li>
           <li id="session"> <%= link_to "Sign In", new_session_path %> </li>
         <% end %>
  	</ul>
    <hr>
  <div class="flash">
    <% if flash[:notice] %>
      <div class="alert alert-success">
        <button type="button" class="close" data-dismiss="alert">&times;</button>
        <%= flash[:notice] %>
      </div>
    <% elsif flash.now[:alert] %>
      <div class="alert alert-danger">
        <button type="button" class="close" data-dismiss="alert">&times;</button>
        <%= flash.now[:alert] %>
      </div>
    <% elsif flash[:alert] %>
      <div class="alert alert-warning">
        <button type="button" class="close" data-dismiss="alert">&times;</button>
        <%= flash[:alert] %>
      </div>
    <% end %>
  </div>
    <%= yield %>
  </div>
</body>
</html>
